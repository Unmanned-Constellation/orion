repos:
  - repo: local
    hooks:
      - id: clang-format
        name: clang-format
        entry: clang-format
        args: [-i]
        language: system
        types_or: [c++, c]

      - id: gersemi
        name: gersemi
        entry: gersemi
        args: [-i]
        language: system
        files: '(CMakeLists\.txt|\.cmake)$'