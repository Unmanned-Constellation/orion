{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Selected CMake Target",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated"
        }
    ]
}